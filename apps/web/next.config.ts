import type { NextConfig } from "next";

// The dashboard talks to the AgentEval API directly from the browser (see NEXT_PUBLIC_API_URL in lib/api.ts).
const config: NextConfig = {};

export default config;
